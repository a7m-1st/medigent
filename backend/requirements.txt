# MedGemma Backend Dependencies
# Generated from pyproject.toml — install with: pip install -r requirements.txt

# Core framework
fastapi>=0.115.12
uvicorn[standard]>=0.34.2
pydantic>=2.0.0

# AI / Agent framework
camel-ai==0.2.85
openai>=1.99.3,<2

# Environment & config
python-dotenv>=1.1.0

# HTTP client
httpx[socks]>=0.28.1

# i18n (installed but not yet wired)
fastapi-babel>=1.0.0
pydantic-i18n>=0.4.5

# Utilities
pydash>=8.0.5
inflection>=0.5.1
aiofiles>=24.1.0
numpy>=1.23.0,<2.0.0

# Node.js runtime (for MCP/browser toolkits)
nodejs-wheel>=22.18.0

# Vector DB client (available, not yet used)
qdrant-client>=1.16.2

# Observability (available, not yet used)
opentelemetry-api>=1.34.1
opentelemetry-sdk>=1.34.1
opentelemetry-exporter-otlp-proto-http>=1.34.1

# Debugging
debugpy>=1.8.17

# Package manager
pip>=23.0
